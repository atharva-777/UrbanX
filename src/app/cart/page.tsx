import SideBar from "@/components/SideBar";
import React from "react";

const page = () => {
  return (
    <div>
      {/* <SideBar sidebarOpen={true} /> */}
    </div>
  );
};

export default page;
